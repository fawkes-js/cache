export interface ICacheClient {
  // init: () => any;
  // get: (id: string) => any;
  // set: (id: string, data: any) => any;
  // has: (id: string) => any;
  // patch: (id: string, data: any) => any;
}
